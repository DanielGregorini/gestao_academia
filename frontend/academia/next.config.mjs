// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'standalone',
    // Outras configurações podem ser adicionadas aqui
  };
  
  export default nextConfig;
  